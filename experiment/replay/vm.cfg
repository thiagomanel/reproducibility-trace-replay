#vm_image_path = "/local/manel/doc/metrology/og-image-v2.qcow2"

#vm_instance = {
#    "small": (2, 3840),
#    "large": (8, 15360)
#}

# qemu $vm_image_path -enable-kvm -smp $vcpu -m $vmem -redir tcp:2222::22 -curses
